properties:
  template:
    volumes:
      - name: scoutdata
        storageName: scoutdata
        storageType: AzureFile
    containers:
      - name: scout-backend
        image: sarralscoutacr.azurecr.io/scout-backend:latest
        volumeMounts:
          - volumeName: scoutdata
            mountPath: /app/data
